<div class="header bg-primary pb-6">
    <div class="container-fluid">
      <div class="header-body">
        <div class="row align-items-center py-4">
          <div class="col-lg-6 col-7">
            <h6 class="h2 text-white d-inline-block mb-0">Curriculum</h6>
            <nav aria-label="breadcrumb" class="d-none d-md-inline-block ml-md-4">
              <ol class="breadcrumb breadcrumb-links breadcrumb-dark">
                <li class="breadcrumb-item"><a href="/"><i class="fas fa-home"></i></a></li>
                <li class="breadcrumb-item"><a href="/curriculum">Curriculum</a></li>
                <li class="breadcrumb-item active" aria-current="page">Curriculum</li>
              </ol>
            </nav>
          </div>
          <div class="col-lg-6 col-5 text-right">
            <a href="/curriculum/add" class="btn btn-sm btn-neutral">New</a>
          </div>
        </div>
      </div>
    </div>
  </div>
  <!-- Page content -->
  <div class="container-fluid mt--6">
    <div class="row">
      <div class="col">
        <div class="card">
          <!-- Card header -->
          <div class="card-header border-0">
            <div class="row">
              <div class="col-9">
                <h3 class="mb-0">Curriculum table</h3>
              </div>
              <div class="col-3">
                <input type="text" id="curriculumInput" class="form-control form-control-alternative" onkeyup="filterCurriculum()" placeholder="🔎 filter curriculum by name">
              </div>
            </div>
          </div>
          <!-- Light table -->
          <div class="table-responsive">
            <table class="table align-items-center table-flush" id="subjectCurriculumTable">
              <thead class="thead-light">
                <tr>
                  <th scope="col" class="sort" data-sort="name">Curriculum ID</th>
                  <th scope="col" class="sort" data-sort="budget">Curriculum Name</th>
                  <th scope="col"></th>
                </tr>
              </thead>
              <tbody class="list">
                <% for(var i=0; i<curriculums.length; i++) { %>
                <tr>
                  <th scope="row">
                    <%= curriculums[i].curriculum_id %>
                  </th>
                  <td class="budget">
                    <div class="media align-items-center">
                        <div class="media-body">
                          <span class="name mb-0 text-sm"><%= curriculums[i].curriculum_name %></span>
                        </div>
                      </div>
                  </td>
                  <td class="text-right">
                    <div class="dropdown">
                      <a class="btn btn-sm btn-icon-only text-light" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-ellipsis-v"></i>
                      </a>
                      <div class="dropdown-menu dropdown-menu-right dropdown-menu-arrow">
                        <a class="dropdown-item" href="/curriculum/edit/<%= curriculums[i].curriculum_id %>">Edit</a>
                        <a class="dropdown-item" href="/curriculum/delete/<%= curriculums[i].curriculum_id %>">Delete</a>
                      </div>
                    </div>
                  </td>
                </tr>
                <% } %>
              </tbody>
            </table>
          </div>
          <!-- Card footer -->
          <!-- <div class="card-footer py-4">
            <nav aria-label="...">
              <ul class="pagination justify-content-end mb-0">
                <li class="page-item disabled">
                  <a class="page-link" href="#" tabindex="-1">
                    <i class="fas fa-angle-left"></i>
                    <span class="sr-only">Previous</span>
                  </a>
                </li>
                <li class="page-item active">
                  <a class="page-link" href="#">1</a>
                </li>
                <li class="page-item">
                  <a class="page-link" href="#">2 <span class="sr-only">(current)</span></a>
                </li>
                <li class="page-item"><a class="page-link" href="#">3</a></li>
                <li class="page-item">
                  <a class="page-link" href="#">
                    <i class="fas fa-angle-right"></i>
                    <span class="sr-only">Next</span>
                  </a>
                </li>
              </ul>
            </nav>
          </div> -->
        </div>
      </div>
    </div>
    <!-- Dark table -->

    <!-- Footer -->
  </div>
<script>
  function filterCurriculum() {
    // Declare variables
    var input, filter, table, tr, td, i, txtValue;
    input = document.getElementById("curriculumInput");
    filter = input.value.toUpperCase();
    table = document.getElementById("subjectCurriculumTable");
    tr = table.getElementsByTagName("tr");
  
    // Loop through all table rows, and hide those who don't match the search query
    for (i = 0; i < tr.length; i++) {
      td = tr[i].getElementsByTagName("td")[0];
      if (td) {
        txtValue = td.textContent || td.innerText;
        if (txtValue.toUpperCase().indexOf(filter) > -1) {
          tr[i].style.display = "";
        } else {
          tr[i].style.display = "none";
        }
      }
    }
  }
</script>